import { type JSX } from 'react';
import { RoutesContainer } from './providers';

export const App = (): JSX.Element => {
    return (
        <>
            <RoutesContainer />
        </>
    );
};

export default App;
