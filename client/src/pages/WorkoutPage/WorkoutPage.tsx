import { type JSX } from 'react';
import { WorkoutForm } from '../../features';

export const WorkoutPage = (): JSX.Element => {
    return (
        <>
            <WorkoutForm />
        </>
    );
};
